controller:
  metrics:
    enabled: true
  service:
    type: NodePort
    nodePorts:
      http: 80
      https: 443
  image:
    repository: registry.k8s.io/ingress-nginx/controller
    tag: "v1.11.1"
    # registry will be set via --set in workflow
  admissionWebhooks:
    patch:
      image:
        repository: registry.k8s.io/ingress-nginx/kube-webhook-certgen
        tag: "v20240424-helm-chart-5.1.0"
        # registry will be set via --set in workflow

